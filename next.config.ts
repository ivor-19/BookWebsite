import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['amethyst-keen-canid-380.mypinata.cloud'], // Allow images from Pinata
  },
};

export default nextConfig;
